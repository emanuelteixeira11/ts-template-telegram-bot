{
  "name": "ts-template-telegram-bot",
  "version": "1.0.0",
  "description": "Typescript template of telegram bot",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node build/index.js"
  },
  "keywords": [
    "telegram",
    "twitter",
    "telegram",
    "to",
    "twitter"
  ],
  "author": "eteixeira",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "telegraf": "^4.4.2"
  },
  "devDependencies": {
    "@types/node": "^16.11.8",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.2"
  }
}
